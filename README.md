# robo

This repository contains a simple robot prototype implemented in Python.

## Requirements

- Python 3.x
- pytest (for running tests)

## Running the prototype

Execute `robot.py` to see the robot move in a simple scenario:

```bash
python robot.py
```

## Running tests

Install `pytest` if necessary and run:

```bash
pytest
```
